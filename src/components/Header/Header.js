import React from "react";
import "./Header.scss";

const Header = () => {
  return (
    <header className="Header">
      <h1>Twitch YouTube Request Bot</h1>
    </header>
  );
};

export default Header;
